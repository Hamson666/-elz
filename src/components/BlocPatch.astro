---
    const { patch } = Astro.props;
---

<img class="patch-img lg:absolute object-cover box-border border-base-color border-2" src=`${ patch.frontmatter.image.url }` alt=`${ patch.frontmatter.image.alt }` />